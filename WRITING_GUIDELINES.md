# 執筆規約

規約版: 2

本書の一貫性を保つための規約。ここに書かれた構造上の規約のうち機械検査できるものは `scripts/lint/` が CI で検査する。

規約版は、規約を 1 条でも足したり書き換えたりしたら 1 つ進める。原稿は書いた時点の規約版で固定されるので、版を進めたら全章を新しい条で読み直し、`docs/prose-review-log.md` の各章の版を更新する。この更新が済むまで `scripts/lint/review-lint.mjs` が落ちる。規約だけが進んで原稿が取り残される状態を、機械で見張るための仕組みである。

検出器が生きているかどうかも検査する。`npm run lint:self` は、原稿に違反を注入して各検出器が実際に落ちることを確かめる。規約を書き換えたり検出器の正規表現を直したりすると、検出器が黙って何も見なくなることがある。そうなると CI の緑は「規約が守られている」ではなく「検査していない」を意味する。検出器を触ったら、この自己検査の probe も合わせて直す。

## 1. 技術選定方針（執筆前に固定する前提）

本書の手順・記述は以下の現行推奨に揃える。旧方式は「レガシーとして存在する」ことだけ短く触れ、手順には含めない。

| 領域                                   | 本書が正とするもの                                                                   | 扱わないもの                                                                |
| -------------------------------------- | ------------------------------------------------------------------------------------ | --------------------------------------------------------------------------- |
| Functions のサーバーレスプラン         | Flex Consumption                                                                     | 従来の Consumption プラン（Linux 版は 2028-09-30 廃止予定）                 |
| ガバナンス配布                         | Template Specs / Deployment Stacks / Azure Policy + 管理グループ + IaC               | Azure Blueprints（2026-07-11 廃止済み）                                     |
| Landing Zone の IaC 実装               | Bicep の Azure Verified Modules (AVM) for Platform Landing Zone                      | ALZ-Bicep クラシック（2026-02-16 にアクセラレータから削除・アーカイブ済み） |
| Key Vault の認可                       | Azure RBAC                                                                           | アクセスポリシー（API 2026-02-01 以降、新規 Vault の既定は RBAC）           |
| AKS の Pod から Azure サービスへの認証 | Microsoft Entra Workload ID（OIDC フェデレーション + ユーザー割り当てマネージド ID） | Pod-managed identity（廃止済み）                                            |
| Cosmos DB の認証                       | `disableLocalAuth` + データプレーン RBAC                                             | キー認証を前提にした構成                                                    |
| ID サービスの名称                      | Entra ID                                                                             | Azure AD（初出時に併記するのみ）                                            |

Deployment Stacks は単なるツール紹介ではなく、「リソースグループ = ライフサイクル境界」という学びの実装として第 11・13 章で正面から扱う。

執筆時は各章の冒頭で該当サービスの公式ドキュメントの最新版を確認し、この方針リスト自体を更新すること。 サービス仕様の変化は速く、本リストも陳腐化する。

## 2. 第 4 部の 6 ブロックの型

第 4 部（第 14〜19 章）は以下の H2 見出しをこの順で持つ。`templates/chapter-part4.md` から生成する。

1. `## 1. このサービスは何のためにあるか`（3 行以内）
2. `## 2. 縦の依存関係`
3. `## 3. 横の繋がり ― 認証認可`
4. `## 4. 横の繋がり ― インフラ足回りとネットワーク`
5. `## 5. 横の繋がり ― 契約・課金`
6. `## 6. ハンズオン`

ブロック 2 では、効かない層があればその理由も書く。ブロック 3 では内向き（このサービスから他へのアクセス）と外向き（このサービスへのアクセス）を分け、RBAC のスコープ選択がガバナンス階層と一致するとは限らない点を毎章明示する。ブロック 5 の末尾には、いま作ったものが課金上どう見えるかを確認する Cost Management のコマンドを 1 つ置く。ブロック 6 には壊す演習とクリーンアップ演習を必ず含め、最後に 1〜5 で説明した概念がどう確認できたかを短く振り返る。

第 1〜3 部、第 5 部の章は、この型のうち該当するブロックだけを使う。無理に 6 ブロック全部を埋めない。

## 3. 全章共通の構造

各章は以下を持つ。`scripts/lint/structure-lint.mjs` が検査する。

- H1 見出しが 1 つだけ
- `## 検証環境` ブロック（後述）
- 末尾に `## 理解度チェック` と設問 3 問

### H1 の書式

`# 第 N 章 主題` または `# 第 N 章 主題 ― サブタイトル` のどちらかで書く。付録は `# 付録 X タイトル`。章番号はファイル名の先頭 2 桁と一致させる。

章をまとめた区分は `第 N 部` と書く。`Part N` と英語で書かない。番号は章と同じく算用数字にする（`第一部` ではなく `第 1 部`）。読者が本文で辿る単位を章と部の 2 つに揃え、そこに英語の第 3 の呼び名を混ぜないため。ディレクトリ名の `manuscript/part1/` は表記の対象外とする。`scripts/lint/structure-lint.mjs` が原稿・README・執筆規約を検査する。

主題の前に `[統合ハンズオン]` のようなラベルを置かない。角括弧は使わない。ラベルは目次の一行としては読めても、章の見出しとしては主題を後ろへ押しやる。企画書の表記が規約を経ずに原稿へ転写された結果として第 4・9・13 章に残っていたものであり、いまは structure-lint が落とす。

部の末尾に置くハンズオン章は `# 第 N 章 ハンズオン ― …` で書く。`統合` のような修飾語を足さない。その章が部の締めであることは目次の並びと本文の「第 1 部はここで閉じます」が既に示しており、タイトルで重ねて言う必要がない。`統合ハンズオン` は `docs/glossary.json` の `bannedPhrases` にあり terms-lint が落とす。

### 理解度チェックの作り方

設問で使う語は、その章の本文にも出しておく。用語集にあり初出章が前であっても、それだけでは足りない。読者は前の章の括弧の中で一度見ただけの語を覚えていない。実際、第 4 章の設問は teardown を前提にしていたが、この語は第 3 章の文末の括弧で一度触れられただけで、第 4 章の本文には一度も出てこず、スクリプトのパスとしてしか現れていなかった。どうしても本文に出せない場合は、設問の中に第 N 章の参照を添える。`scripts/lint/terms-lint.mjs` が検査する。

暗記の再現ではなく応用を問う。ただし、その章の本文とハンズオンで扱った範囲だけで解けるものにする。未習の章の知識を前提にした設問を置かない（どうしても未習概念に触れる場合は、設問の中で必要な情報を与えるか、第 N 章の参照を添える）。本文でやっていない操作を題材にするときは、本文で確認した性質から推論できる形に限る。

- 良い例: 「Flex Consumption の Function App をリソースグループごと削除すると、ユーザー割り当てマネージド ID も一緒に消えるか。消えない場合、どこに残るか」
- 避ける例: 「Functions のホスティングプランを 3 つ挙げよ」（暗記の確認に留まり、繋がりの理解を測れない）

### 検証環境ブロック

`scripts/record-verification.sh` が生成する形式。手書きしない。

```markdown
## 検証環境

| 項目           | 値                                            |
| -------------- | --------------------------------------------- |
| 検証状態       | verified / pending / blocked                  |
| 検証日         | 2026-08-08                                    |
| Azure CLI      | 2.77.0                                        |
| Bicep CLI      | 0.37.4                                        |
| API バージョン | Microsoft.Resources/resourceGroups@2025-04-01 |
```

`blocked` は権限やクォータの都合で検証できなかったことを示す。検証していない手順を verified として書かない。

## 4. Mermaid の規約

図は GFM ネイティブの Mermaid で書く。GitHub は ```mermaid フェンスをそのままレンダリングする。

- 使う図種は GitHub がサポートするものに限る: `flowchart`, `sequenceDiagram`, `stateDiagram-v2`, `erDiagram`, `classDiagram`
- テーマ指定・`%%{init}%%` ディレクティブ・外部プラグインは使わない
- 括弧を含むラベルは必ず `"` で囲む。 日本語ラベル中の `(` `)` `（` `）` は無引用だと GitHub のパーサを壊す。`scripts/lint/mermaid-lint.mjs` がこの規則だけを検査する
- 図のノードには種類を明記する（「prod」ではなく「prod (本番) 管理グループ」のように名前 + 種類で書く）。名前だけのノードは、読者にそれが何なのかを推測させてしまう
- 図に出るサービス名・ロール名は、その図より前の本文で導入しておく。図はその章で最初に読者の目に入る場所であり、そこに初見の固有名を置くと、読者はまず「これは何だ」で止まる。導入は 1 文でよく、扱う章への参照を添える（「Key Vault はパスワードや証明書を預ける保管庫で第 16 章で扱います」）。前の章で既に出た語はそのまま使ってよい。terms-lint が図のラベルの英語の固有名を検査し、前の章の本文にも図より前の本文にも用語集にも無ければ落とす
- 1 図につきノード 12 個以内。超えるなら図を分ける
- Mermaid で描き切れる図は直接描く。スクリーンショット相当など描き切れないものだけ `ここに図: (図の内容)` の 1 行で残し、後工程で図版を作る

良い例:

```text
flowchart TB
  S["サブスクリプション (課金の境界)"] --> R["リソースグループ"]
```

壊れる例（括弧が無引用）:

```text
flowchart TB
  S[サブスクリプション (課金の境界)] --> R[リソースグループ]
```

## 5. 執筆上の注意点

- 業界の口語を使わない。「名前で引く」「値を引く」は、検索なのか取得なのか名前解決なのかが読者に伝わらない。取得する・解決する・問い合わせる・引用する、のうち実際に起きていることを書く。`scripts/lint/structure-lint.mjs` が検査する
- 「掛ける」を使わない。「権限をスコープに掛ける」「予算を仕掛ける」「ロックを掛ける」は、割り当てなのか適用なのか設定なのか作成なのかが語から分からない。実際の操作を書く（ロール割り当てなら割り当てる、ポリシーなら適用する、ロックなら設定する、予算なら作成する）。「掛け算」も同じで、ロールとスコープの関係は組み合わせと書く。費用が「掛かる」は通常の日本語なので対象外。`scripts/lint/structure-lint.mjs` が検査する
- 同じ操作は、同じ段落の中で言い換えない。第 6 章は「ロールの割り当ては、階層のどこにでも掛けられます」と書いていて、1 文の中で割り当てと掛けるが混在していた。読者は 2 つが同じ操作なのか別の操作なのかを判断できない。節の見出しと本文を揃える規則と同じ理由で、動詞も 1 つに固定する
- リソースが「生まれる」と書かない。「作られる」「できる」「出る」で書く。テナントもサブスクリプションもキーも生き物ではなく、作られるものである。「キーを持って生まれます」は「作成した時点でキーを持っています」と書く。`scripts/lint/structure-lint.mjs` が検査する
- 概念の説明のあとに必ず具体的な操作を置く。抽象だけで段落を終えない
- 事実を出し惜しみしない。「実は」「意外に感じる事実を 1 つ」のような前置きは、読者に身構えさせてから内容を渡す形で、1 手余分になる。「作成したてのテナントにも、既存のサービスプリンシパルが大量に定義されています」のように、知ってほしいことを最初の文で言い切る。読者は謎解きを求めて読んでいない
- リソースや ID を擬人化しない。存在は「いる」ではなく「ある」または「定義されている」、動作は「振る舞う」ではなく「動作する」、複数の側面は「2 つの顔」ではなく「2 つの側面」、依存関係は「相棒」ではなく「依存先」、寿命の一致は「運命を共にする」ではなく「同時に消える」と書く。助数詞も人や生き物のものを使わない（サービスプリンシパルが 47 体 → 47 種類）。擬人化は一度は分かった気にさせるが、対象が実際に何であるかを覆い隠す。読者がコマンドの出力と本文を突き合わせるとき、比喩は手掛かりにならない。禁止語は `docs/glossary.json` の `bannedPhrases` に置き、terms-lint が検査する
- 章や節の主題を、抽象名詞ひとつで名付けない。「本章で固定したいのは 1 つのねじれです」と書くと、読者は「ねじれとは何か」から考えることになり、そのあとに続く説明を読むまで意味が定まらない。何と何がどういう関係にあるのかを平叙文で書く（「ID の実体はテナントに属し、リソースはサブスクリプションに属します」）。名前を付けたくなったら、その名前が読者に何を思い浮かべさせるかを確かめる。`docs/glossary.json` の `bannedPhrases` に、この形で使われた語（ねじれ・歪み・手触り・勘所）を入れてある
- 新しい概念は「できること」「存在理由」「最小の具体例（数字があれば数字で）」の順で書く。抽象語や造語で定義した気にならない。難しい語を使う場合は初出で一言定義する（例: 統制 = ルールと権限で組織を縛ること）
- 概念に本書だけの比喩の造語を与えない。既存の語で書けるなら造語を作らない。第 5 章は、他社のアプリのサービスプリンシパルが自分のテナントの台帳に作られることを「投影」と呼び、その定義を括弧の中に「本体は別の場所にあり、このテナント用の写しが台帳に置かれる」と置いていた。読者は「投影」の意味を推し量る手間を負ったうえ、3 段落あとの「この投影された ID」が何を指すのかを掴めなかった。既に定義済みの語（テナントの台帳、台帳の 1 行、サービスプリンシパル）で同じ内容が書ける。造語を作りたくなったら、まず既存の語で書けないかを疑う。禁止語は `docs/glossary.json` の `bannedPhrases` に置き、terms-lint が検査する
- 枠組み語の定義は独立した文で書く。括弧の中や図のラベルの中に定義を置かない。読者は括弧を補足として読み飛ばすため、そこにしか定義が無いと語だけが残る
- 普通の意味を持つ語を専門用語として使う章では、両方の意味を並べて分ける。第 5 章は「アプリケーション」を、プログラムという普通の意味（「アプリケーション自身も Azure の API を呼びます」）と、Entra ID の台帳に置かれる登録内容という専門用語の意味の両方で使い、後者の定義は括弧の中の「設計図」という比喩 1 語だけだった。同じ語が同じ章で 2 通りに使われていることを読者は疑わないので、書き手が明示的に分ける。この規則は機械検査できない
- 各章に図を最低 1 枚置く（structure-lint が強制する）。主要概念の構造・対比・流れは、文章でこねる前に図で示す
- 章の主要テーマは、その章だけを読んだ初見の読者が理解できるかを自問する。前提が要るなら第 0 章の基本用語の表か該当章への参照で埋める
- 他クラウドとの対応表や比較は本文に出さない。 読者が Azure 外の知識を持っているかどうかに依存しない書き方にする
- サービスの機能を並べるだけの節を作らない。「なぜその構造になっているか」を必ず一文添える
- 手順のコマンドは 1 ステップ 1 コードブロックで示し、説明はコメントではなく本文に書く（structure-lint が bash ブロック内の行頭コメントを落とす。行末の短い補足は許可）。コピペの単位を小さく保つため。1 ステップの数え方は structure-lint が持つ。変数への代入と、その変数をすぐ使う 1 手はまとめて 1 ステップ。`export` も後続の 1 手と同じステップ。それ以外の独立したコマンドは別のブロックに分け、間に何をするのかを 1 文書く。作る・確認する・消すは別のステップであり、順序に意味がある teardown では特にそう扱う
- シェル変数は、使うブロックより前のブロックで値を取る。本文は章の頭から順に貼れば通る列でなければならず、代入が `scripts/` の中にしか無い状態は、読者の手元で空文字が渡ることを意味する（structure-lint が検査する）
- 章のコマンドは、その章の中で作ったものだけを対象にする。他の章の成果物への依存を前提にしない。リソースグループは各章が自分で作る（structure-lint が別章リソースの参照と group create の欠落を検査する）
- 出力を貼るときは、検証環境で生まれた識別子をダミーに置き換える。テナント ID・サブスクリプション ID・ユーザーやサービスプリンシパルやマネージド ID のオブジェクト ID・UPN が対象で、個人や組織に辿り着く手がかりになる。組み込みロール定義 ID や組み込みポリシー定義 ID は Azure が公開している値なので実値のままでよい。ダミーは、同じ実値が同じダミーになるよう写す。第 5 章の「appId は同じなのに id が違う」のように、値の一致そのものが説明になっている箇所を壊さないため。伏せた実値は `scripts/lint/structure-lint.mjs` の拒否リストへ追加する
- 出力を載せるかどうかは、その出力が読者に何かを教えるかで決める。載せるのは次の 3 つ。第 1 に、出力そのものが本文の主張を裏付けるとき（第 4 章の管理グループ作成は、親が Tenant Root Group であることと、返る型が `managementGroups/subscriptions` であることが本文の説明そのものになる）。第 2 に、成否を読者が自分で判断できないと先へ進めないとき。第 3 に、実測すると意外な値が出るとき。逆に、`az group create` の JSON のように成功が自明で読みどころの無い出力は載せない。ページを埋めるだけで、どこを見ればよいのかが分からなくなる
- 作ったものは、章の中で 1 度は確かめる手を置き、その確認コマンドの出力を載せる。作成のたびに出力を貼るのではなく、確認の手に出力を集めるほうが読者は追いやすい。各章に実出力を最低 1 つ置くことは structure-lint が検査する
- ポータルの操作は、書くときも直すときも、その都度ブラウザで実際の画面を開いて確かめる。新規に書く場合だけの規則ではない。1 行だけ直す場合も、前に見たときの記憶や以前の原稿を根拠にしない。Azure ポータルの UI は頻繁に変わり、タブの構成、開いた直後に選ばれているタブ、列の名前、日本語での表示名が改版で入れ替わる。第 4 章には、現行のポータルに存在しないチェックボックスを操作させる手順が残っていて、読者が画面上で探すことになった
- 確かめる対象は、画面に出る文字すべてである。左メニューの項目名、タブの名前と並び、既定で選ばれているタブ、表の列名、値の表記。CLI で使う名前とポータルの表示が違う場合（`Storage Blob Data Contributor` と「ストレージ BLOB データ共同作成者」）は、同じものだと本文で断る。この規則は機械検査できない。ポータルの手順を含む段落に触れたら、画面を開くところから始める
- 実測で踏んだ失敗も載せる。エラーの全文、それが何を意味するか、成功しているのに失敗に見える場合の見分け方（別のコマンドで確かめる手順）、本当に駄目なときにどう先へ進むかの 4 点を書く。エラーを伏せると、読者は自分の環境が壊れていると考える
- 本文は執筆者自身が実行して得た出力だけを元に書く。先に実行し、その出力を説明する形で書く。他者から報告された出力は自分で再現してから載せ、再現できない場合はその旨と出所を明記する。因果の推定を事実の形で書かない
- レガシー方式は「存在したこと・なぜ置き換わったか」を 1 段落で触れるに留め、手順は現行推奨のみで書く
- コストは無料枠と低額 SKU で完結する構成を前提にする。無料枠を超えうる箇所は章の冒頭に概算を明記する
- 日本語はですます調で書く。専門用語とコード識別子は原語のまま。対句や短い言い切りで効果を出したくなる場面があるが、地の文では使わない（「権限は足りている、それでも作れない」→「権限は足りているのに作れません」、「CLI で理解し、IaC で固定する」→「…固定します」）。文の調子が段落ごとに変わると、読者は書き手の距離感を測り直すことになる。鉤括弧で囲んだ引用の中は話し手の言葉なので対象外。`scripts/lint/structure-lint.mjs` が検査する
- 読者の理解に向けた指示は肯定形で書く。「別のロールだと思わないでください」は、読者に一度そう思わせてから打ち消させる形で、余計な手数が要る。「この 2 つを同じロールとして読んでください」と、してほしい読み方を直接書く。「読み違えないでください」も同じで、「どこを見て判断するか」を書けば否定は要らなくなる。ただし禁止は否定形が正しい。「顧客環境や本番サブスクリプションでは実行しないでください」は、実行してよい場所を挙げても禁止にならない。事実の否定（「削除は作成の逆順ではありません」）もそのままでよい。structure-lint は、理解に関する動詞（思う・考える・読み違える・勘違いする・混同する・取り違える）の否定形の依頼だけを落とす
- ASCII と日本語の境目には半角スペースを 1 つ置く（第 4 章、付録 B、照会自体が 404、4 階層、Azure CLI 2.77.0 の設定）。数字や英字が地の文に埋もれると語の切れ目が読めなくなるため。全角の約物（、。（）「」・）との隣接は詰める。実行するコマンドと、実際の出力を貼ったブロックは記録なので手を入れない。`scripts/lint/structure-lint.mjs` が検査する
- 英語の概念語は「日本語の語 +（英語）」の対訳形式で書く（論理削除（soft-delete）、物理削除（purge）、ゼロトラスト（Zero Trust））。日本語の定訳があるのに英語だけで書かない。見出しも同じ。コマンド・プロパティ・製品名（purge コマンド、EnableServerless、Flex Consumption）はそのままでよい
- 定義していない語・略語を使わない。図のラベルでも同じとし、例に使う固有の名前も図の前に一言導入する。本書が繰り返し使う枠組み語（効く・内向き/外向き・コントロールプレーン/データプレーン・teardown・ワークロードなど）は初出で定義したうえで docs/glossary.json に初出章を登録し、terms-lint に順序を守らせる。見出しに枠組み語を使うのは、本文で定義済みの章以降に限る。以降の規則は略語について（RP、MG、SP のような略しは全文どこにも書かない。RBAC や IaC のように業界で固有名詞として通っているものは第 0 章の基本用語の表か初出で定義してから使う）
- 本文で太字（`**`）を使わない。どこが重要かは読者の成長段階によって変わるものであり、書き手が強調で固定しない。`scripts/lint/structure-lint.mjs` が機械検査する
- 読者は Azure 初心者を想定する。基本用語（ユーザー、Entra ID、テナント、サブスクリプションなど）の定義は第 0 章の図解が引き受け、以降の章はそこを参照する
- 絵文字は使わない

## 6. Operation as Code の運用

本文に長いコマンド列を直接埋め込まない。`scripts/chapters/chNN-*.sh` に実体を置き、本文はその一部を引用しながら説明する。読者が実行する対象と CI が検査する対象を一致させるため。

| スクリプト                   | 責務                                                                 |
| ---------------------------- | -------------------------------------------------------------------- |
| `scripts/chapters/chNN-*.sh` | 章の手順そのもの。冪等ではない（作る）                               |
| `scripts/verify/chNN-*.sh`   | 期待状態の read-only 検証。冪等。RBAC の伝播遅延を考慮しリトライする |
| `scripts/teardown/chNN-*.sh` | 後始末。soft-delete されるリソースは purge まで行う                  |

すべてのスクリプトは `set -euo pipefail` で始め、`scripts/lib/common.sh` を読み込む。shellcheck を通すこと。原稿や生成物を書き換えるスクリプトは、書き換えた対象の整形（prettier）まで済ませて終わること。整形前のファイルを残すと CI の format 検査と食い違う。

## 8. 語を決める 3 層のフィルター

本書の指摘のうち、繰り返し出たものは語の選び方だった。投影、載せる、掛ける、ねじれ。どれも、その場で言い換えて禁止語に足して終わりにしてきた。指摘の数だけ禁止語が増える運用は、次の造語を止めない。語は無限に作れるためである。

そこで、語を抽象度の高い側から順に検査する。上の層で止まったものは下の層まで来ない。下の層に語を足すのは、上の 2 層で止められなかったと分かったときだけにする。

### 層 1: 方針（何を語彙にするか）

操作を表す語は、Azure が公式に使う語から採る。`az` のコマンド名、フラグ名、公式ドキュメントの日本語表記のいずれかに実在する語だけを使う。実在しない語は、どれだけ分かりやすく見えても使わない。読者はコマンドの出力と本文を突き合わせながら読むので、本書にしか無い語は照合の手掛かりにならない。

正典は `docs/vocabulary.json` の `operations` に置く。各行は、対象（object）・動詞（verb）・出典（source）・避ける語（avoid）を持つ。出典の無い行は `scripts/lint/vocab-lint.mjs` が落とす。出典を書けない語は、本書の語彙に入れない。

比喩の導入句（いわば、たとえるなら、見立て）も同じ層で落とす。本書の造語は、いずれもこの形の文から作られた。比喩で説明したくなったら、対象そのものの説明が足りていない。

### 層 2: 文脈（その語をこの章で読者がどう受け取るか）

語を新しく決める行為そのものを検査する。「〜と呼びます」の形で語を決めたら、その語を `docs/glossary.json` の `terms` へ初出章つきで登録する。登録は、その語を本書が今後ずっと抱えるという判断を明示する手続きである。登録の無い宣言は落ちる。

多義語は、使う章ごとにその場で注釈する。コンテナーのように、Azure 自身が 2 つの意味で使う語がある。用語集への登録も前の章での定義も、いま開いている章の読者には届かない。

### 層 3: 語（最後の網）

`docs/vocabulary.json` の `denied` が言い換え表である。語・言い換え先・理由・担当する検出器を 1 行で持つ。新しく落としたい語は、コードではなくこの表に足す。

語彙スナップショット（`docs/vocabulary-snapshot.txt`）は、本書がいま使っている語の一覧である。ここに無い語が原稿に出ると `vocab-lint` が落ちる。増やすときは `npm run vocab:update` が差分を表示するので、その一覧を見て造語が混ざっていないかを確かめる。見ないで通すなら、この仕組みは無いのと同じである。

### 指摘が来たときの順序

指摘を受けたら、まず層 1 で止められなかったかを問う。次に層 2、最後に層 3。層 3 に語を 1 つ足して終わりにすると、同じ形の造語が別の章で出る。

## 7. 踏み抜きやすい罠（既知）

執筆時に必ず織り込む。

- Key Vault の soft-delete: 削除しても名前は約 90 日予約される。teardown に `az keyvault purge` を含めないと章が再実行不能になる（第 16 章）
- Cosmos DB の Free Tier はサブスクリプションに 1 つだけ。再実行で壊れるため Free Tier を使わず serverless で書く（第 18 章）
- 管理グループの削除は空でないと失敗する。teardown は順序依存になる（第 4 章）
- Flex Consumption の対応リージョンは一覧を持つが、2026-08 実測で 52 リージョン（日本 2 リージョン含む）まで拡大済み。確認コマンドは置くが「限定」と強調しない（第 14 章）
- RBAC 割り当ての伝播遅延。verify スクリプトはリトライ付きにする（第 6・9 章）
- **未登録プロバイダーの壊す演習は `az provider unregister` で作らない**。対象リソースが存在すると unregister が失敗し、演習が再現不能になる。検証時に実際に未登録のプロバイダーを選ぶ（第 1 章）
